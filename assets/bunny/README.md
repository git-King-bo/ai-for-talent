# Bunny Placeholder Image Assets

These are generated placeholder PNG expressions kept as fallback/reference assets.

The active animated action frames live in `assets/bunnyBase`.

Placeholder defaults:

- `normal.png`
- `shy.png`
- `happy.png`

Use `npm run assets` or `pnpm assets` to regenerate these placeholders.
